@model Sport_Match.Dtos.RegisterUserDto

@{
    ViewData["Title"] = "Registracija";
}

<div class="auth-page">
    <div class="auth-card">

        <h1 class="auth-title">SportMatch</h1>
        <p class="auth-subtitle">Registriraj se i pridruži ekipi!</p>

        <form asp-action="Register" method="post">

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <input asp-for="Email" class="form-control auth-input" placeholder="E-mail" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <input asp-for="Password" type="password" class="form-control auth-input" placeholder="Lozinka" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <input asp-for="ConfirmPassword" type="password" class="form-control auth-input" placeholder="Ponovi lozinku" />
                <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
            </div>

            <button type="submit" class="auth-btn auth-btn-primary">
                Registriraj se
            </button>

            <a asp-controller="Account" asp-action="Login" class="auth-btn-secondary">
                Već imaš račun? Prijavi se
            </a>

        </form>

    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
s